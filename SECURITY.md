# 🔐 ГАЙД БЕЗОПАСНОСТИ - ЧТО НЕЛЬЗЯ КОМИТИТЬ

## ❌ НИКОГДА не отправляйте эти файлы:

### 🔑 Ключи и пароли:
- `.env` - переменные окружения с DATABASE_URL, SMTP_PASS и т.д.
- `.env.local`, `.env.*.local` - локальные конфиги
- `*.key` - private ключи
- `*.secret` - файлы с секретами
- `auth.json`, `credentials.json` - учетные данные

### 🗄️ Базы данных:
- `*.db`, `*.sqlite`, `*.sqlite3` - локальные БД
- `node_modules/` - npm зависимости (уже в .gitignore)

### 📋 Логи:
- `*.log` - логфайлы
- `npm-debug.log*` - ошибки npm
- `lerna-debug.log*` - ошибки build'а

---

## ✅ КАК РАБОТАЕТ ЗАЩИТА:

### 1. **.gitignore** (исключение файлов)
```
.env              ← Автоматически исключается
.env.local        ← Не добавится в git
*.key             ← Ключи не пройдут
credentials.json  ← Учетные данные защищены
```

### 2. **Скрипт quick-deploy.bat** (проверка перед пушем)
- ⚠️ Сканирует перед комитом
- 🔐 Если найдет `.env` - автоматически исключит
- ✅ Показывает "Безопасность OK"

---

## 🛡️ ЧТО ДЕЛАТЬ:

### Если случайно добавили секреты:

```powershell
# 1. Исключить из последнего коммита:
git reset HEAD filename.env

# 2. Удалить из истории (ВАЖНО!):
git filter-branch --tree-filter 'rm -f .env' HEAD

# 3. Force push (очень осторожно!):
git push --force origin main
```

### Если собираетесь работать с .env:
```powershell
# Создайте локальный .env - он НЕ будет отправлен
touch .env

# Заполните его локально - это не будет видно в гите
echo DATABASE_URL=... > .env
```

---

## 📋 ТЕКУЩАЯ ПРОВЕРКА

Чувствительные файлы в .gitignore:
✅ .env
✅ .env.local
✅ *.key
✅ *.secret
✅ auth.json
✅ credentials.json
✅ node_modules/
✅ *.log файлы

---

## 🚀 БЕЗОПАСНЫЙ ДЕПЛОЙ:

Просто используйте:
```
./quick-deploy.bat "Your message"
```

Скрипт автоматически:
1. Проверит что нет .env файлов
2. Служит что нет *.key файлов
3. Проверит что нет других секретов
4. ✅ После проверки отправит на GitHub

**Если нашел опасность** → **ОТМЕНИТ коммит** ⛔

---

## ⚠️ ДОПОЛНИТЕЛЬНЫЕ СОВЕТЫ:

### Для Railway на производстве:
- Используйте **Railway Environment Variables** 
- НЕ коммитьте .env в git
- Railway автоматически добавит переменные при деплое

### Для локальной разработки:
- Создайте `.env.local` для тестирования
- Добавляйте в .gitignore
- Railway проигнорирует локальный файл

---

## 📞 ЕСЛИ ВСЕ ЖЕ ПРОИЗОШЛА УТЕЧКА:

1. **Немедленно** смените пароли в Railway
2. **Регенерируйте** API ключи
3. Используйте `git filter-branch` чтобы удалить из истории
4. Force push на GitHub

---

**Помните:** Git отправляет ВСЮ историю! После push на GitHub - всё видно!

🔒 Проверяйте перед пушем!
